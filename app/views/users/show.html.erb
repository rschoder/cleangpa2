<div class="page-header">
  <h3>User #<%= @user.id %> (<%=@user.email%>)</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <dl class="dl-horizontal">


      <dt>User</dt>
      <dd><%= @user.email %></dd>

   <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Course name</th>
        <th>Year Taken</th>
      </tr>

      <tr>
      <td><% @grades.each do |grade|%>
        <dd><%=grade.course_id%></dd>
      <%end%>
      </td>
      <td><% @grades.each do |grade|%>

      <dd><%=grade.schoolyear%></dd>
      <%end%>
    </td>
    </tr>

    </table>

    </dl>

    </div>
  </div>
</div>
