<div class="page-header">
  <h3>User #<%= @user.id %> (<%=@user.email%>)</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <dl class="dl-horizontal">


      <dt>User</dt>
      <dd><%= @user.email %></dd>

 <table class="table table-hover table-responsive col-md-2">
    <thead>
    Courses
    </thead>

    <th>
      <tr><% @grades.each do |grade|%>
        <dd><%=grade.course_id%></dd></tr>
      <%end%>
    </th>
    <th>
      <% @grades.each do |grade|%>

      <dd><%=grade.schoolyear%></dd>
      <%end%>
    </th>


    </table>



      <dt>Schoolyear</dt>


    </dl>

    </div>
  </div>
</div>
